export default {
  id: 'math-notation',
  name: '数学符号',
  icon: '∑',
  blurb: '论文看不懂，八成是卡在符号上：⊤ 是转置、𝔼 是期望、∇ 是梯度、‖x‖ 是范数。这里把论文里最常见的符号按主题整理成速查表，每个都给大白话解释和一个具体例子。看论文时遇到不认识的符号回来查，比跳过它强一百倍。',

  templates: [
    {
      id: 'set-logic',
      title: '集合与逻辑符号',
      lang: 'python',
      tags: ['基础', '必背'],
      why: '论文第一页定义部分全是这些。不认识 ∈ 和 ⊂，连"这个方法适用什么数据"都读不出来。',
      code: `x ∈ A        # x 属于集合 A：x 是 A 里的一个元素。如 3 ∈ {1,2,3}
x ∉ A        # x 不属于 A
A ⊆ B        # A 是 B 的子集：A 的元素全在 B 里（可以相等）
A ⊂ B        # A 是 B 的真子集：A ⊆ B 但 A ≠ B
A ∪ B        # 并集：在 A 或 B 里。|A ∪ B| = |A| + |B| - |A ∩ B|
A ∩ B        # 交集：同时在 A 和 B 里
A \\ B 或 A ∖ B  # 差集：在 A 里但不在 B 里
A^c 或 ¬A    # 补集/非：全集里除去 A 的部分
|A|          # 集合的大小（元素个数），也写作 card(A)
∅            # 空集
∀x           # 对所有 x（任意）
∃x           # 存在至少一个 x
: 或 |       # "使得"，定义集合用：{x ∈ X : f(x) > 0} = X 中让 f(x)>0 的那些 x
f: X → Y     # f 是从集合 X 映射到集合 Y 的函数
N = {1,2,3,…}   Z 整数   R 实数   R^n n 维实向量   R^(m×n) m行n列实矩阵`,
      points: [
        '∈ 读"属于"，⊆ 读"包含于"，方向别记反：x ∈ A 是元素对集合，A ⊆ B 是集合对集合',
        '{x : 条件} 竖线前是元素，竖线后是筛选条件，整读作"所有满足条件的 x"',
        '|A| 在概率论文里几乎总是"集合大小"，别和绝对值搞混——看里面装的是什么',
      ],
      pitfalls: [
        '⊂ 和 ⊆ 有的论文不区分真子集，看它自己的记号约定（通常在开头的 Notation 节）',
        'R^n 是 n 维"列向量"的默认记法，(R^m)^n 或 R^(m×n) 才是矩阵',
      ],
    },

    {
      id: 'sum-prod',
      title: '求和、连乘与下标',
      lang: 'python',
      tags: ['基础', '必背'],
      why: '论文里 90% 的公式开头都是 Σ。看不懂 Σ 的上下标，公式直接读不了。其实它就是个 for 循环。',
      code: `Σ（sigma，求和）      Π（pi，连乘）

n
Σ  a_i   =  a_1 + a_2 + … + a_n        # i 从 1 数到 n，把每一项加起来
i=1

n
Π  a_i   =  a_1 × a_2 × … × a_n       # 全部乘起来（如极大似然）
i=1

x ∈ S
Σ  f(x)  =  把集合 S 里每个元素代进 f 再全加
x

Σ_i        # 省略写法，上下标被吞了，意思是"对所有 i 求和"
Σ_i p_i log p_i = -H   # 信息熵就是这么来的

# 对照代码：
# total = 0
# for i in range(1, n+1): total += a[i]     ← 这就是 Σ
# prod = 1
# for i in range(1, n+1): prod *= a[i]      ← 这就是 Π`,
      points: [
        '下面是循环起点，上面是终点；只有下标没上标 = 对全部可能值求和',
        'Σ 后面跟着的式子是"循环体"，先算式子再累加',
        '机器学习里 Σ 常和对数据集求和混用：Σ_(x,y)∈D 表示对数据集 D 里每个样本对求和',
        'log 和 Σ 的次序是很多公式的关键：log Σ ≠ Σ log（先加后取对数 ≠ 先取对数再加）',
      ],
      pitfalls: [
        '连乘 Π 和求和 Σ 形状很像，先看清是哪个——极大似然里 Π 转成 log 才变 Σ',
        '上下标写的是"哪个变量在循环"，正文里换变量名时公式不变，别被吓到',
      ],
    },

    {
      id: 'vector-matrix',
      title: '向量与矩阵记号',
      lang: 'python',
      tags: ['线性代数', '必背'],
      why: 'Transformer 论文里满屏的 QK⊤、√d_k、W ∈ R^(d×d)。认不出这些记号，Attention 公式就是天书。',
      code: `x ∈ R^n        # x 是 n 维列向量（竖着写）
x_i            # 向量 x 的第 i 个分量
A ∈ R^(m×n)    # m 行 n 列矩阵
A_ij           # 矩阵 A 第 i 行第 j 列的元素
x⊤             # 转置：行向量变列向量。x⊤y = x·y（内积/点积）
A⊤             # 矩阵转置：行列互换，(A⊤)_ij = A_ji
‖x‖            # 范数：向量长度。默认 ‖x‖_2 = sqrt(Σ x_i²)（欧氏距离）
‖x‖_1          # 各分量绝对值之和（L1，稀疏化用）
‖x‖_∞          # 最大的那个分量的绝对值
x·y 或 ⟨x,y⟩   # 内积（点积）= Σ x_i·y_i = cosθ·‖x‖‖y‖
A x            # 矩阵乘向量：结果还是向量
tr(A)          # 迹：对角线元素之和
det(A)         # 行列式
I              # 单位矩阵（对角线是 1 其余 0）
A⁻¹            # 逆矩阵：A·A⁻¹ = I（类比数字的倒数）

# Attention 里的 QK⊤/√d_k 逐个拆开就是：
# Q,K: (L,d) 矩阵；K⊤: (d,L)；QK⊤: (L,L) 每个位置是一对 token 的点积；
# 除 √d 是把点积的方差压回 1，防止 softmax 饱和`,
      points: [
        '⊤ 是 transpose 的标记，看到 X⊤Y 就想"逐位置点积"，这是算相似度的标准动作',
        '‖x‖ 不写下标时默认 L2 范数；L1/L2 之差是正则化章节的主角',
        '矩阵乘法形状要对上：(m×n)·(n×p)→(m×p)，中间那个 n 必须相等',
      ],
      pitfalls: [
        '有的论文用小写粗体 x 表示向量、大写 A 表示矩阵、花体 X 表示随机变量/集合——开头约定要看',
        'x⊤x 是标量（一个数），xx⊤ 是矩阵（外积），长得像但完全是两个东西',
      ],
    },

    {
      id: 'probability',
      title: '概率与统计符号',
      lang: 'python',
      tags: ['概率', '必背'],
      why: '贝叶斯公式那串 P(θ|D) ∝ P(D|θ)P(θ) 是生成模型和贝叶斯方法的地基。每个符号都认识，连起来才不慌。',
      code: `P(A)           # 事件 A 发生的概率
P(A|B)         # 条件概率：在 B 已经发生的条件下 A 的概率
P(A,B) 或 P(A∩B)  # 联合概率：A 和 B 同时发生
p(x)           # 随机变量的分布/密度（小写 p 常表示密度，大写 P 表示概率）
p(x|y) = p(x,y) / p(y)      # 条件概率定义
X ~ N(μ, σ²)   # X 服从均值 μ、方差 σ² 的正态（高斯）分布
~              # "服从"（某个分布）
𝔼[X] 或 E[X]   # 期望：随机变量的平均值 = Σ x·p(x)（连续时用积分）
Var[X]         # 方差 = 𝔼[(X-𝔼[X])²] = 𝔼[X²] - 𝔼[X]²
Cov(X,Y)       # 协方差
⊥ 或 ⫫         # 独立：X ⊥ Y 表示 X 和 Y 相互独立
x₁,…,xₙ ~ i.i.d. p(x)   # 独立同分布采样：每个样本都从同一个分布独立抽出
argmax_x f(x)  # 让 f(x) 最大的那个 x（返回的是取值点，不是最大值）
argmin_x f(x)  # 让 f(x) 最小的 x

# 贝叶斯定理（后验 ∝ 似然 × 先验）：
# P(θ|D) = P(D|θ)·P(θ) / P(D)
#          └似然┘  └先验┘   └证据（归一化常数）`,
      points: [
        'P(A|B) 读作"给定 B 时 A 的概率"，竖线右边是"已经知道的条件"',
        '𝔼[X] 是"长期平均"：把每个取值按概率加权平均，论文里所有"期望损失"都是它',
        'argmax 返回的是"哪个 x"不是"多大值"；max 才返回值。两者常出现在同一公式里',
        '∝（正比于）表示"差一个和 θ 无关的常数因子"，贝叶斯推导里靠它跳过分母计算',
      ],
      pitfalls: [
        'p(x|y) 和 p(y|x) 是两个完全不同的东西，混淆它们是贝叶斯公式用错的头号原因',
        'i.i.d. 是"独立同分布"，不是"某个分布的名字"——它是对采样方式的假设声明',
      ],
    },

    {
      id: 'gradient-optim',
      title: '梯度、导数与优化记号',
      lang: 'python',
      tags: ['优化', '必背'],
      why: '训练相关的论文（loss 设计、优化器、微调）全是 ∇θ、∂L/∂θ、η。认不出这些就看不懂训练过程在干嘛。',
      code: `f'(x) 或 df/dx    # 一元函数的导数：变化率
∂f/∂x            # 偏导数：多元函数里只看 x 方向的变化率（其他变量当常数）
∇f 或 grad f     # 梯度：把所有偏导数装进一个向量，指向上升最快的方向
∇_θ L            # 损失 L 对参数 θ 的梯度（下标 θ 表示"对谁求"）
∇²f 或 H         # Hessian 矩阵：二阶导数，描述曲率
θ ← θ - η·∇_θ L  # 梯度下降一行：沿负梯度方向挪一小步
η 或 lr          # 学习率：每步走多远
θ, θ*            # 参数；θ*（星号）表示最优解/最终解
λ                # 正则化系数：L = L_data + λ·R(θ)，λ 越大越怕过拟合
L(θ) 或 J(θ)     # 损失函数（Loss/Objective），有的论文用 J
O(·)             # 渐近复杂度：O(n²) = 平方级增长
O(L·d²)          # 常见于分析 Transformer 每层计算量

# 训练循环对照：
# for step in range(T):
#     g = grad(L, theta)          # ∇_θ L
#     theta = theta - lr * g      # θ ← θ - η∇_θ L`,
      points: [
        '∇ 是"对所有参数求偏导再打包"，它是个向量，方向是上升最快方向，所以下降要减',
        '∂ 是"偏"的意思：只动一个变量，其他全按住不动',
        'λ 和 η 是论文调参章节的两个常客：λ 管正则强度，η 管步子大小',
        '星号上标 θ* 是"最优的那个"，hat 上标 x̂ 是"估计/预测值"，波浪 x̃ 是"加噪/扰动后的"',
      ],
      pitfalls: [
        '∇_θ 的下标容易被忽略——对 loss 求梯度还是对某个中间量求梯度，含义完全不同',
        'O(·) 省略了常数和低阶项，O(n) 的算法不一定比 O(n²) 快，小规模下常数项说了算',
      ],
    },

    {
      id: 'information-theory',
      title: '信息论常用记号',
      lang: 'python',
      tags: ['信息论', '常见'],
      why: 'KL 散度、交叉熵、困惑度是语言模型论文的三件套。H 和 D_KL 认不出来，loss 那一栏就看不懂。',
      code: `H(p)          # 熵：分布 p 的不确定性。H = -Σ p(x)·log p(x)，越大越"乱"
H(p,q)        # 交叉熵：用 q 去编码真实分布 p 的平均代价 = -Σ p(x)·log q(x)
D_KL(p ‖ q)   # KL 散度：两个分布的"距离"（不对称）= Σ p(x)·log(p(x)/q(x))
I(X;Y)        # 互信息：知道 X 能减少多少关于 Y 的不确定性
 perplexity   # 困惑度 = exp(交叉熵)，语言模型的通用报告指标

# 语言模型 loss 的真相（分类视角）：
# 交叉熵损失 = -log p(正确的下一个 token)
# 模型给正确答案的概率越接近 1，loss 越接近 0

# D_KL 的直觉：p 是真实分布，q 是模型猜的分布
# D_KL = 0 表示猜得完美；猜错的"惩罚"按 p 的权重累计
# 注意 D_KL(p‖q) ≠ D_KL(q‖p)，方向不同含义不同（前向 vs 反向 KL）`,
      points: [
        '交叉熵是分类任务默认的 loss：类别数越少公式越短，二分类时就是 -[y·log p + (1-y)·log(1-p)]',
        'D_KL 里的 ‖ 不是除号，是 KL 散度记号的一部分，读作"p 对 q 的 KL 散度"',
        '困惑度和交叉熵是同一件事的两种说法，指数关系，报告里常互换',
      ],
      pitfalls: [
        'log 在 ML 论文里默认是自然对数 ln（信息论有时以 2 为底）——困惑度计算时底数错了差一个常数倍',
        'KL 散度不对称：fit "平均化" 的 q 用反向 KL，fit "集中" 的 q 用前向 KL，蒸馏论文常纠缠这一点',
      ],
    },

    {
      id: 'paper-formula-patterns',
      title: '论文常见公式套路',
      lang: 'python',
      tags: ['套路', '常见'],
      why: '单个符号都认识，整条公式还是读不懂——因为论文有一批反复出现的"公式句型"。认出句型，公式就读出声了。',
      code: `# 1. softmax —— 把任意分数变成"和为 1 的概率"
#    softmax(z)_i = e^{z_i} / Σ_j e^{z_j}
#    读法：每个位置取指数（拉开差距），再除以总和（归一化）

# 2. 极大似然估计 MLE —— 挑一个"最可能生成这些数据"的参数
#    θ* = argmax_θ Π_i p(x_i | θ)  →  取 log 变连加：
#    θ* = argmax_θ Σ_i log p(x_i | θ)   ← 实际优化的是这个（负号一加就是 loss）

# 3. 期望损失 —— 训练就是在最小化它
#    L(θ) = 𝔼_(x,y)~D [ ℓ(f_θ(x), y) ]
#    读法：从数据 D 里抽 (x,y)，看模型猜的 f_θ(x) 和答案 y 差多少（ℓ 是差多少），取平均

# 4. 正则化 —— 数据项 + 惩罚项
#    min_θ  Σ ℓ(...) + λ‖θ‖²     ← L2 正则（权重衰减），怕参数太大
#    min_θ  Σ ℓ(...) + λ‖θ‖₁     ← L1 正则，想把参数压稀疏

# 5. 条件生成 —— 自回归语言模型的本质
#    p(x_1,…,x_T) = Π_t p(x_t | x_<t)
#    读法：整句话的概率 = 每个 token 在"看过前文"条件下的概率连乘

# 6. 重参数化/采样技巧 —— VAE 论文那一串
#    z ~ N(μ, σ²)  写成  z = μ + σ·ε,  ε ~ N(0,1)
#    读法：随机性从 ε 来，z 对 μ、σ 的梯度就能算了

# 7. 上标不是次方的情况
#    f^(t)      # 第 t 次迭代的结果（上下文能判断出来）
#    x^(i)      # 第 i 个样本；x_i 是第 i 个维度——看论文自己的约定`,
      points: [
        '读长公式的顺序：先认形状（Σ？argmax？期望？），再认作用对象，最后看上下标约束',
        '见到 Π 想极大似然，见到 argmax 想训练目标，见到 λ‖θ‖ 想正则化，见到 Π_t p(x_t|…) 想自回归',
        '带帽子的 x̂ 是预测/估计，波浪 x̃ 是扰动/加噪，星号 x* 是最优——上标记号自成一套语言',
      ],
      pitfalls: [
        'e^{z_i} 的花括号是分组不是格式装饰；e 的指数里再套分数时先看清分子分母',
        '同一篇论文里 i/j/t 含义可能换来换去，"Notation"一节给出的约定优先于你的直觉',
      ],
    },
  ],
};
