export default {
  id: 'math-notation',
  name: '数学符号',
  icon: '∑',
  blurb: '论文看不懂，八成是卡在符号上：⊤ 是转置、𝔼 是期望、∇ 是梯度、‖x‖ 是范数。这里把论文里最常见的符号按主题整理成速查表，每个都给大白话解释和一个具体例子。看论文时遇到不认识的符号回来查，比跳过它强一百倍。',

  templates: [
    {
      id: 'set-logic',
      title: '集合与逻辑符号',
      lang: 'python',
      tags: ['基础', '必背'],
      why: '论文第一页定义部分全是这些。不认识 ∈ 和 ⊂，连"这个方法适用什么数据"都读不出来。',
      code: `x ∈ A        # x 属于集合 A：x 是 A 里的一个元素。如 3 ∈ {1,2,3}
x ∉ A        # x 不属于 A
A ⊆ B        # A 是 B 的子集：A 的元素全在 B 里（可以相等）
A ⊂ B        # A 是 B 的真子集：A ⊆ B 但 A ≠ B
A ∪ B        # 并集：在 A 或 B 里。|A ∪ B| = |A| + |B| - |A ∩ B|
A ∩ B        # 交集：同时在 A 和 B 里
A \\ B 或 A ∖ B  # 差集：在 A 里但不在 B 里
A^c 或 ¬A    # 补集/非：全集里除去 A 的部分
|A|          # 集合的大小（元素个数），也写作 card(A)
∅            # 空集
∀x           # 对所有 x（任意）
∃x           # 存在至少一个 x
: 或 |       # "使得"，定义集合用：{x ∈ X : f(x) > 0} = X 中让 f(x)>0 的那些 x
f: X → Y     # f 是从集合 X 映射到集合 Y 的函数
N = {1,2,3,…}   Z 整数   R 实数   R^n n 维实向量   R^(m×n) m行n列实矩阵`,
      points: [
        '∈ 读"属于"，⊆ 读"包含于"，方向别记反：x ∈ A 是元素对集合，A ⊆ B 是集合对集合',
        '{x : 条件} 竖线前是元素，竖线后是筛选条件，整读作"所有满足条件的 x"',
        '|A| 在概率论文里几乎总是"集合大小"，别和绝对值搞混——看里面装的是什么',
      ],
      pitfalls: [
        '⊂ 和 ⊆ 有的论文不区分真子集，看它自己的记号约定（通常在开头的 Notation 节）',
        'R^n 是 n 维"列向量"的默认记法，(R^m)^n 或 R^(m×n) 才是矩阵',
      ],
    },

    {
      id: 'sum-prod',
      title: '求和、连乘与下标',
      lang: 'python',
      tags: ['基础', '必背'],
      why: '论文里 90% 的公式开头都是 Σ。看不懂 Σ 的上下标，公式直接读不了。其实它就是个 for 循环。',
      code: `Σ（sigma，求和）      Π（pi，连乘）

n
Σ  a_i   =  a_1 + a_2 + … + a_n        # i 从 1 数到 n，把每一项加起来
i=1

n
Π  a_i   =  a_1 × a_2 × … × a_n       # 全部乘起来（如极大似然）
i=1

x ∈ S
Σ  f(x)  =  把集合 S 里每个元素代进 f 再全加
x

Σ_i        # 省略写法，上下标被吞了，意思是"对所有 i 求和"
Σ_i p_i log p_i = -H   # 信息熵就是这么来的

# 对照代码：
# total = 0
# for i in range(1, n+1): total += a[i]     ← 这就是 Σ
# prod = 1
# for i in range(1, n+1): prod *= a[i]      ← 这就是 Π`,
      points: [
        '下面是循环起点，上面是终点；只有下标没上标 = 对全部可能值求和',
        'Σ 后面跟着的式子是"循环体"，先算式子再累加',
        '机器学习里 Σ 常和对数据集求和混用：Σ_(x,y)∈D 表示对数据集 D 里每个样本对求和',
        'log 和 Σ 的次序是很多公式的关键：log Σ ≠ Σ log（先加后取对数 ≠ 先取对数再加）',
      ],
      pitfalls: [
        '连乘 Π 和求和 Σ 形状很像，先看清是哪个——极大似然里 Π 转成 log 才变 Σ',
        '上下标写的是"哪个变量在循环"，正文里换变量名时公式不变，别被吓到',
      ],
    },

    {
      id: 'vector-matrix',
      title: '向量与矩阵记号',
      lang: 'python',
      tags: ['线性代数', '必背'],
      why: 'Transformer 论文里满屏的 QK⊤、√d_k、W ∈ R^(d×d)。认不出这些记号，Attention 公式就是天书。',
      code: `x ∈ R^n        # x 是 n 维列向量（竖着写）
x_i            # 向量 x 的第 i 个分量
A ∈ R^(m×n)    # m 行 n 列矩阵
A_ij           # 矩阵 A 第 i 行第 j 列的元素
x⊤             # 转置：行向量变列向量。x⊤y = x·y（内积/点积）
A⊤             # 矩阵转置：行列互换，(A⊤)_ij = A_ji
‖x‖            # 范数：向量长度。默认 ‖x‖_2 = sqrt(Σ x_i²)（欧氏距离）
‖x‖_1          # 各分量绝对值之和（L1，稀疏化用）
‖x‖_∞          # 最大的那个分量的绝对值
x·y 或 ⟨x,y⟩   # 内积（点积）= Σ x_i·y_i = cosθ·‖x‖‖y‖
A x            # 矩阵乘向量：结果还是向量
tr(A)          # 迹：对角线元素之和
det(A)         # 行列式
I              # 单位矩阵（对角线是 1 其余 0）
A⁻¹            # 逆矩阵：A·A⁻¹ = I（类比数字的倒数）

# Attention 里的 QK⊤/√d_k 逐个拆开就是：
# Q,K: (L,d) 矩阵；K⊤: (d,L)；QK⊤: (L,L) 每个位置是一对 token 的点积；
# 除 √d 是把点积的方差压回 1，防止 softmax 饱和`,
      points: [
        '⊤ 是 transpose 的标记，看到 X⊤Y 就想"逐位置点积"，这是算相似度的标准动作',
        '‖x‖ 不写下标时默认 L2 范数；L1/L2 之差是正则化章节的主角',
        '矩阵乘法形状要对上：(m×n)·(n×p)→(m×p)，中间那个 n 必须相等',
      ],
      pitfalls: [
        '有的论文用小写粗体 x 表示向量、大写 A 表示矩阵、花体 X 表示随机变量/集合——开头约定要看',
        'x⊤x 是标量（一个数），xx⊤ 是矩阵（外积），长得像但完全是两个东西',
      ],
    },

    {
      id: 'probability',
      title: '概率与统计符号',
      lang: 'python',
      tags: ['概率', '必背'],
      why: '贝叶斯公式那串 P(θ|D) ∝ P(D|θ)P(θ) 是生成模型和贝叶斯方法的地基。每个符号都认识，连起来才不慌。',
      code: `P(A)           # 事件 A 发生的概率
P(A|B)         # 条件概率：在 B 已经发生的条件下 A 的概率
P(A,B) 或 P(A∩B)  # 联合概率：A 和 B 同时发生
p(x)           # 随机变量的分布/密度（小写 p 常表示密度，大写 P 表示概率）
p(x|y) = p(x,y) / p(y)      # 条件概率定义
X ~ N(μ, σ²)   # X 服从均值 μ、方差 σ² 的正态（高斯）分布
~              # "服从"（某个分布）
𝔼[X] 或 E[X]   # 期望：随机变量的平均值 = Σ x·p(x)（连续时用积分）
Var[X]         # 方差 = 𝔼[(X-𝔼[X])²] = 𝔼[X²] - 𝔼[X]²
Cov(X,Y)       # 协方差
⊥ 或 ⫫         # 独立：X ⊥ Y 表示 X 和 Y 相互独立
x₁,…,xₙ ~ i.i.d. p(x)   # 独立同分布采样：每个样本都从同一个分布独立抽出
argmax_x f(x)  # 让 f(x) 最大的那个 x（返回的是取值点，不是最大值）
argmin_x f(x)  # 让 f(x) 最小的 x

# 贝叶斯定理（后验 ∝ 似然 × 先验）：
# P(θ|D) = P(D|θ)·P(θ) / P(D)
#          └似然┘  └先验┘   └证据（归一化常数）`,
      points: [
        'P(A|B) 读作"给定 B 时 A 的概率"，竖线右边是"已经知道的条件"',
        '𝔼[X] 是"长期平均"：把每个取值按概率加权平均，论文里所有"期望损失"都是它',
        'argmax 返回的是"哪个 x"不是"多大值"；max 才返回值。两者常出现在同一公式里',
        '∝（正比于）表示"差一个和 θ 无关的常数因子"，贝叶斯推导里靠它跳过分母计算',
      ],
      pitfalls: [
        'p(x|y) 和 p(y|x) 是两个完全不同的东西，混淆它们是贝叶斯公式用错的头号原因',
        'i.i.d. 是"独立同分布"，不是"某个分布的名字"——它是对采样方式的假设声明',
      ],
    },

    {
      id: 'gradient-optim',
      title: '梯度、导数与优化记号',
      lang: 'python',
      tags: ['优化', '必背'],
      why: '训练相关的论文（loss 设计、优化器、微调）全是 ∇θ、∂L/∂θ、η。认不出这些就看不懂训练过程在干嘛。',
      code: `f'(x) 或 df/dx    # 一元函数的导数：变化率
∂f/∂x            # 偏导数：多元函数里只看 x 方向的变化率（其他变量当常数）
∇f 或 grad f     # 梯度：把所有偏导数装进一个向量，指向上升最快的方向
∇_θ L            # 损失 L 对参数 θ 的梯度（下标 θ 表示"对谁求"）
∇²f 或 H         # Hessian 矩阵：二阶导数，描述曲率
θ ← θ - η·∇_θ L  # 梯度下降一行：沿负梯度方向挪一小步
η 或 lr          # 学习率：每步走多远
θ, θ*            # 参数；θ*（星号）表示最优解/最终解
λ                # 正则化系数：L = L_data + λ·R(θ)，λ 越大越怕过拟合
L(θ) 或 J(θ)     # 损失函数（Loss/Objective），有的论文用 J
O(·)             # 渐近复杂度：O(n²) = 平方级增长
O(L·d²)          # 常见于分析 Transformer 每层计算量

# 训练循环对照：
# for step in range(T):
#     g = grad(L, theta)          # ∇_θ L
#     theta = theta - lr * g      # θ ← θ - η∇_θ L`,
      points: [
        '∇ 是"对所有参数求偏导再打包"，它是个向量，方向是上升最快方向，所以下降要减',
        '∂ 是"偏"的意思：只动一个变量，其他全按住不动',
        'λ 和 η 是论文调参章节的两个常客：λ 管正则强度，η 管步子大小',
        '星号上标 θ* 是"最优的那个"，hat 上标 x̂ 是"估计/预测值"，波浪 x̃ 是"加噪/扰动后的"',
      ],
      pitfalls: [
        '∇_θ 的下标容易被忽略——对 loss 求梯度还是对某个中间量求梯度，含义完全不同',
        'O(·) 省略了常数和低阶项，O(n) 的算法不一定比 O(n²) 快，小规模下常数项说了算',
      ],
    },

    {
      id: 'information-theory',
      title: '信息论常用记号',
      lang: 'python',
      tags: ['信息论', '常见'],
      why: 'KL 散度、交叉熵、困惑度是语言模型论文的三件套。H 和 D_KL 认不出来，loss 那一栏就看不懂。',
      code: `H(p)          # 熵：分布 p 的不确定性。H = -Σ p(x)·log p(x)，越大越"乱"
H(p,q)        # 交叉熵：用 q 去编码真实分布 p 的平均代价 = -Σ p(x)·log q(x)
D_KL(p ‖ q)   # KL 散度：两个分布的"距离"（不对称）= Σ p(x)·log(p(x)/q(x))
I(X;Y)        # 互信息：知道 X 能减少多少关于 Y 的不确定性
 perplexity   # 困惑度 = exp(交叉熵)，语言模型的通用报告指标

# 语言模型 loss 的真相（分类视角）：
# 交叉熵损失 = -log p(正确的下一个 token)
# 模型给正确答案的概率越接近 1，loss 越接近 0

# D_KL 的直觉：p 是真实分布，q 是模型猜的分布
# D_KL = 0 表示猜得完美；猜错的"惩罚"按 p 的权重累计
# 注意 D_KL(p‖q) ≠ D_KL(q‖p)，方向不同含义不同（前向 vs 反向 KL）`,
      points: [
        '交叉熵是分类任务默认的 loss：类别数越少公式越短，二分类时就是 -[y·log p + (1-y)·log(1-p)]',
        'D_KL 里的 ‖ 不是除号，是 KL 散度记号的一部分，读作"p 对 q 的 KL 散度"',
        '困惑度和交叉熵是同一件事的两种说法，指数关系，报告里常互换',
      ],
      pitfalls: [
        'log 在 ML 论文里默认是自然对数 ln（信息论有时以 2 为底）——困惑度计算时底数错了差一个常数倍',
        'KL 散度不对称：fit "平均化" 的 q 用反向 KL，fit "集中" 的 q 用前向 KL，蒸馏论文常纠缠这一点',
      ],
    },

    {
      id: 'paper-formula-patterns',
      title: '论文常见公式套路',
      lang: 'python',
      tags: ['套路', '常见'],
      why: '单个符号都认识，整条公式还是读不懂——因为论文有一批反复出现的"公式句型"。认出句型，公式就读出声了。',
      code: `# 1. softmax —— 把任意分数变成"和为 1 的概率"
#    softmax(z)_i = e^{z_i} / Σ_j e^{z_j}
#    读法：每个位置取指数（拉开差距），再除以总和（归一化）

# 2. 极大似然估计 MLE —— 挑一个"最可能生成这些数据"的参数
#    θ* = argmax_θ Π_i p(x_i | θ)  →  取 log 变连加：
#    θ* = argmax_θ Σ_i log p(x_i | θ)   ← 实际优化的是这个（负号一加就是 loss）

# 3. 期望损失 —— 训练就是在最小化它
#    L(θ) = 𝔼_(x,y)~D [ ℓ(f_θ(x), y) ]
#    读法：从数据 D 里抽 (x,y)，看模型猜的 f_θ(x) 和答案 y 差多少（ℓ 是差多少），取平均

# 4. 正则化 —— 数据项 + 惩罚项
#    min_θ  Σ ℓ(...) + λ‖θ‖²     ← L2 正则（权重衰减），怕参数太大
#    min_θ  Σ ℓ(...) + λ‖θ‖₁     ← L1 正则，想把参数压稀疏

# 5. 条件生成 —— 自回归语言模型的本质
#    p(x_1,…,x_T) = Π_t p(x_t | x_<t)
#    读法：整句话的概率 = 每个 token 在"看过前文"条件下的概率连乘

# 6. 重参数化/采样技巧 —— VAE 论文那一串
#    z ~ N(μ, σ²)  写成  z = μ + σ·ε,  ε ~ N(0,1)
#    读法：随机性从 ε 来，z 对 μ、σ 的梯度就能算了

# 7. 上标不是次方的情况
#    f^(t)      # 第 t 次迭代的结果（上下文能判断出来）
#    x^(i)      # 第 i 个样本；x_i 是第 i 个维度——看论文自己的约定`,
      points: [
        '读长公式的顺序：先认形状（Σ？argmax？期望？），再认作用对象，最后看上下标约束',
        '见到 Π 想极大似然，见到 argmax 想训练目标，见到 λ‖θ‖ 想正则化，见到 Π_t p(x_t|…) 想自回归',
        '带帽子的 x̂ 是预测/估计，波浪 x̃ 是扰动/加噪，星号 x* 是最优——上标记号自成一套语言',
      ],
      pitfalls: [
        'e^{z_i} 的花括号是分组不是格式装饰；e 的指数里再套分数时先看清分子分母',
        '同一篇论文里 i/j/t 含义可能换来换去，"Notation"一节给出的约定优先于你的直觉',
      ],
    },
    {
      id: 'greek-and-decorations',
      title: '希腊字母与上下标',
      lang: 'python',
      tags: ['基础', '必背'],
      why: 'AI 论文会用 θ、η、λ、ε、μ、σ 代替一大批参数。它们通常对应参数、学习率、正则、噪声、均值和方差。',
      code: [
        'α alpha       # 权重、混合比例、衰减系数',
        'β beta        # 动量、混合系数、KL 权重',
        'γ gamma       # 缩放系数、折扣因子、LayerNorm 参数',
        'δ delta       # 变化量、小扰动、误差',
        'ε epsilon     # 很小的正数，防止除零：1 / (x + ε)',
        'η eta         # 学习率：θ ← θ - η∇L',
        'λ lambda      # 正则化权重：L = L_data + λR(θ)',
        'μ mu          # 均值，或注意力里的某个参数',
        'σ sigma       # 标准差；σ² 是方差',
        'θ theta       # 待学习的参数整体',
        'φ / ψ         # 另一组参数，常表示编码器/策略参数',
        'x̂ / x_hat    # 估计值或预测值',
        'x̄ / x_bar     # 平均值',
        'x̃ / x_tilde  # 扰动、近似或变换后的值',
        'x* / x_star   # 最优解',
        'x^(t)         # 第 t 步/第 t 层/第 t 个版本，取决于上下文',
        'x_i           # 第 i 个分量或第 i 个样本，必须看 Notation',
      ].join('\n'),
      points: [
        '先看变量角色，不要把 θ、η、λ 当成固定数学常数：论文通常会在开头重新定义它们',
        '上标不一定是次方：x^(i) 常是第 i 个样本，h^(l) 常是第 l 层隐藏状态',
        'ε 通常只是数值稳定项；它不是误差本身，看到除法或开方时优先考虑这一点',
        '帽子、横线、波浪线、星号分别常表示预测、平均、扰动、最优，但最终以本文约定为准',
      ],
      pitfalls: [
        'σ 可能表示标准差、sigmoid 函数或求和符号 sigma，必须结合它后面是否有括号或下标判断',
        '同一篇论文可能用 α 做混合系数、用 β 做正则系数，不能只凭字母背答案',
      ],
    },

    {
      id: 'calculus-integral-limit',
      title: '积分、极限与连续变化',
      lang: 'python',
      tags: ['微积分', '必背'],
      why: '从离散训练样本到总体风险、从概率密度到期望，都要把加起来推广成积分。看懂 ∫ 和 lim，才知道公式是在算平均、面积还是极限。',
      code: [
        'df/dx              # 导数：x 改一点，f 改多少',
        '∂f/∂x              # 偏导：只让 x 变化，其他变量固定',
        '∫_a^b f(x) dx      # 从 a 到 b 的积分：连续版本的求和',
        '𝔼[f(X)] = ∫ f(x)p(x)dx  # 对随机变量取期望',
        'lim_(x→∞) f(x)     # x 趋向无穷时 f(x) 的极限',
        '∫ p(x)dx = 1       # 概率密度在整个空间的面积必须为 1',
        '',
        '# 从经验风险到总体风险：',
        '# (1/n) Σ_i ℓ(f_θ(x_i), y_i)  →  𝔼_(x,y)~D[ℓ(f_θ(x), y)]',
        '# 左边是有限样本平均，右边是数据分布上的理论平均',
        '',
        '# 链式法则：反向传播的数学骨架',
        '# ∂L/∂x = (∂L/∂y) · (∂y/∂x)',
        '# 多层网络就是把每一层的局部导数乘起来',
      ].join('\n'),
      points: [
        'dx 不是乘法里的普通 x：它表示对 x 的无穷小变化或积分变量',
        'Σ 是离散求和，∫ 是连续求和；概率密度要乘 dx 才能变成一小段概率',
        '链式法则把最终损失对输入的影响拆成每一跳的局部影响，这就是反向传播',
        '总体风险是理论期望，经验风险是训练集平均；机器学习通常用后者近似前者',
      ],
      pitfalls: [
        '∫ f(x)dx 不等于只对 f 做一次求和，积分还包含区间和变量的含义',
        '概率密度 p(x) 本身不是单点概率，连续变量的单点概率通常为 0，区间概率要积分',
      ],
    },

    {
      id: 'tensor-shapes-broadcast',
      title: '张量、维度与广播',
      lang: 'python',
      tags: ['深度学习', '必背'],
      why: 'AI 论文很多看不懂其实是没读出每个张量的形状。先追踪 batch、序列长度和 hidden size，公式就从符号变成了数据流。',
      code: [
        'x ∈ R^(B×T×D)       # batch B、序列 T、特征维 D 的三维张量',
        'W ∈ R^(D×H)         # 线性层权重，D 输入维映射到 H 输出维',
        'xW ∈ R^(B×T×H)      # 最后两维做矩阵乘法，B/T 保留',
        'x + b                # 广播：b ∈ R^H 加到每个 token 的特征维',
        'A ⊙ B                # Hadamard 逐元素乘法，形状通常相同',
        'A @ B                # 矩阵乘法，不是逐元素乘',
        'reshape(B,T,D)       # 只要元素总数相同，可以改变视图形状',
        'concat(x, y, dim=-1) # 在最后一维拼接，D 变成 D_x + D_y',
        'mean(x, dim=1)       # 沿序列维求平均，B×T×D → B×D',
        'sum(x, dim=-1)       # 沿最后特征维求和，B×T×D → B×T',
      ].join('\n'),
      points: [
        '形状记号通常按 B×T×D 读：先问多少样本，再问每个样本多少 token，最后问每个 token 多宽',
        '广播不是凭空复制新信息，而是把低维张量按规则扩展到可计算的形状',
        '@ 是矩阵乘法，⊙ 是逐元素乘法；两者输出形状和含义完全不同',
        'dim=-1 通常指最后一维，论文和代码里经常用它避开具体维度编号',
      ],
      pitfalls: [
        '同一个 x 在论文不同段落可能代表单个向量、一个 batch 或一个序列，必须跟着形状看',
        'reshape 不会改变元素总数；transpose 或 permute 会改变维度顺序，二者不要混淆',
      ],
    },

    {
      id: 'attention-qkv-mask',
      title: 'Attention 的 Q、K、V 与掩码',
      lang: 'python',
      tags: ['Transformer', '必背'],
      why: 'Transformer 论文的核心公式反复出现 Q、K、V、d_k、M 和 softmax。把每个符号对应到提问、匹配、取值、屏蔽，就能读懂注意力。',
      code: [
        'Q = XW_Q           # Query：当前位置想找什么',
        'K = XW_K           # Key：每个位置提供什么匹配线索',
        'V = XW_V           # Value：匹配后真正取回的信息',
        'S = QK^T / √d_k    # 相似度分数；除 √d_k 控制数值尺度',
        'A = softmax(S)      # 每行变成和为 1 的注意力权重',
        'Y = A V             # 按权重混合所有 Value',
        'M_ij ∈ {0, -∞}      # mask：允许看写 0，禁止看写 -∞',
        'A = softmax(S + M)  # softmax 后被屏蔽位置权重约等于 0',
        '',
        '# causal attention：第 t 个 token 不能偷看未来',
        '# M_ij = 0  if j ≤ i',
        '# M_ij = -∞ if j > i',
        '',
        '# 形状：X(B,T,D), Q/K/V(B,T,H), QK^T(B,T,T)',
      ].join('\n'),
      points: [
        'Q 是我在找什么，K 是我是否匹配，V 是匹配后拿什么内容，三者通常由同一个 X 的不同投影得到',
        'QK^T 产生 T×T 的两两相关性矩阵：第 i 行表示第 i 个位置关注谁',
        'mask 通常加在 softmax 前；写 -∞ 是为了让 softmax 后对应概率变成 0',
        '多头注意力只是把 H 拆成多个 head，各自在较小维度上计算，再拼回去',
      ],
      pitfalls: [
        'QK^T 和 KQ^T 不是一回事：前者行对应 query，后者形状和语义都变了',
        'mask 的 0/1 约定有时不同；真正关键是被屏蔽位置在 softmax 后权重为 0',
      ],
    },

    {
      id: 'sequence-token-notation',
      title: '序列、Token 与索引记号',
      lang: 'python',
      tags: ['语言模型', '常见'],
      why: '语言模型论文把一句话拆成 x_1 到 x_T，再用 x_<t、h_t、y_t 描述每一步。认出这些索引，生成和训练目标会清楚很多。',
      code: [
        'x = (x_1, x_2, …, x_T)     # 长度 T 的 token 序列',
        'x_<t = (x_1, …, x_(t-1))    # t 之前的所有 token',
        'x_≤t = (x_1, …, x_t)        # 包含当前位置',
        'p(x_t | x_<t)                # 看前文预测第 t 个 token',
        'p(x_1:T) = Π_t p(x_t|x_<t)  # 整句概率按时间步连乘',
        'h_t = f_θ(h_(t-1), x_t)     # 第 t 步隐藏状态',
        'ŷ_t                           # 模型在 t 步的预测',
        'y_t                           # 真实标签',
        '[CLS], [SEP]                 # 特殊 token：分类起点、句子分隔',
        'T                             # 序列长度，不一定是时间',
      ].join('\n'),
      points: [
        '冒号范围 x_1:T 通常是从 1 到 T 的整段序列；x_<t 是严格小于 t，不含当前 token',
        '自回归模型的不能看未来来自条件概率只依赖 x_<t',
        'ŷ 是预测，y 是真实标签；训练交叉熵通常比较这两者',
        'T 在文本里多是 token 数，在视频或时间序列里才更像时间长度',
      ],
      pitfalls: [
        'x_t 可能表示 token，也可能表示第 t 帧或时间步；看数据类型和上下文',
        '论文有时从 t=0 开始编号，不能默认所有下标都从 1 开始',
      ],
    },

    {
      id: 'optimization-constraints',
      title: '优化目标与约束符号',
      lang: 'python',
      tags: ['优化', '必背'],
      why: '论文里的 min、max、s.t.、∈、≤ 共同说明要改变谁、让什么变好、必须满足什么限制。',
      code: [
        'θ* = argmin_θ L(θ)       # 找到让损失最小的参数 θ*',
        'max_θ  f(θ)               # 让目标函数尽可能大',
        'min_θ  L(θ)  s.t.  g(θ)≤0 # subject to：在约束下最小化',
        'θ ∈ Θ                      # θ 必须属于可行参数集合 Θ',
        'x ≥ 0                      # 非负约束',
        '||x||_2 ≤ 1                # 范数球约束',
        'sup_x f(x)                 # 上确界：可能不一定取得到',
        'inf_x f(x)                 # 下确界',
        '≈                           # 近似相等，不是严格等于',
        ':= 或 ≜                    # 定义为：左边是右边的新名字',
        '∝                           # 正比于：差一个与当前变量无关的常数',
      ].join('\n'),
      points: [
        'argmin 返回哪个 θ，min 返回最小损失是多少；这是论文最常见的区分题',
        's.t. 是 subject to 的缩写，后面是必须满足的约束，不是第二个优化目标',
        'Θ 是可行域：算法不是在所有数学对象里乱找，而是在允许的参数范围里找',
        ':= 表示定义，= 表示相等；作者用 := 是在告诉你后面可以直接用这个新记号',
      ],
      pitfalls: [
        '最大化 reward 和最小化 loss 可能只是同一个目标换了符号，不要被 min/max 表面吓到',
        '≈、∝、=、:= 的强度不同：推导时把它们混成等号会丢掉重要条件',
      ],
    },

    {
      id: 'loss-regularization',
      title: '损失函数与正则化符号',
      lang: 'python',
      tags: ['深度学习', '必背'],
      why: '读训练方法时，最重要的问题不是公式长不长，而是它惩罚了什么、鼓励了什么。损失项和正则项通常一眼就能拆开。',
      code: [
        'L_total = L_task + λ L_reg       # 任务损失 + 正则化',
        'L_CE = -Σ_t log p_θ(y_t|x_<t)   # token 交叉熵',
        'L_MSE = (1/n)Σ_i (ŷ_i-y_i)^2     # 均方误差',
        'L_KL = D_KL(p || q)               # 两个分布的差异',
        'L = (1-α)L_main + αL_aux         # 主任务和辅助任务混合',
        'weight decay = λ||θ||_2^2        # 抑制参数过大',
        'L1 = λ||θ||_1                     # 鼓励参数稀疏',
        'margin = max(0, m - y·f(x))       # hinge loss 的间隔惩罚',
        'stop_gradient(z)                  # z 参与前向，但不沿它反传梯度',
        '∇_θ L                             # 对参数 θ 求损失梯度',
      ].join('\n'),
      points: [
        'L_total 里加号后的每一项都是一种训练压力：主任务让它做对，正则项限制它不要走极端',
        'λ 和 α 控制不同损失的相对重量，数值越大不一定越好，要结合量纲和归一化看',
        '交叉熵关注正确答案的概率，MSE 关注数值距离，KL 关注分布形状',
        'stop_gradient 是前向使用、反向切断，常用于稳定训练或避免模块被另一项目标带偏',
      ],
      pitfalls: [
        'L2 正则和 AdamW 的 weight decay 在实现上不总是完全等价，论文要看具体优化器',
        'loss 越小只说明在当前损失定义下更好，不代表所有指标或真实任务表现都更好',
      ],
    },

    {
      id: 'sampling-distributions-estimation',
      title: '采样、分布与估计',
      lang: 'python',
      tags: ['概率', 'AI论文'],
      why: '生成模型、扩散模型、VAE 和强化学习里经常写 z~p、x∼q、Monte Carlo。它们描述的是从哪里抽样和如何用样本近似期望。',
      code: [
        'x ~ p(x)                  # 从分布 p(x) 采样 x',
        'z ~ q_φ(z|x)              # 编码器给出的近似后验',
        'ε ~ N(0, I)               # 标准高斯噪声',
        'z = μ_φ(x) + σ_φ(x)⊙ε     # reparameterization trick',
        'E[f(x)] ≈ (1/n)Σ_i f(x_i) # Monte Carlo：样本平均近似期望',
        'p_data                    # 真实数据分布',
        'p_θ                       # 参数为 θ 的模型分布',
        'q                         # proposal / approximate distribution',
        'x′ 或 x⁺                  # 变换、增强或下一状态，具体看本文定义',
        'Var[估计量]              # 估计结果本身的波动',
        'unbiased: E[估计量] = 真值 # 无偏估计',
      ].join('\n'),
      points: [
        '~ 读作服从或采样自，不是约等于；它说明随机变量的来源',
        'Monte Carlo 的核心是用有限个独立样本平均替代理论期望，样本越多通常越稳定',
        '重参数化把随机性集中到 ε，让 z 对 μ、σ 的梯度仍可计算',
        'p 往往是目标或真实分布，q 往往是近似、提议或采样分布，但必须看论文定义',
      ],
      pitfalls: [
        'q 不一定比 p 差，它可能是为了让采样或推导可计算而引入的辅助分布',
        '样本平均是估计，不是理论期望本身；论文里的 ≈ 往往就在提醒这一点',
      ],
    },

    {
      id: 'matrix-special-ops',
      title: '矩阵特殊运算',
      lang: 'python',
      tags: ['线性代数', '常见'],
      why: 'AI 论文会把矩阵运算压缩成几个符号：⊗、⊙、tr、diag、rank。知道它们是在逐元素、拼接、取对角线还是扩展维度，公式就不会串。',
      code: [
        'A⊙B                 # Hadamard 逐元素乘法',
        'A⊗B                 # Kronecker 积：块矩阵式扩展',
        'A^T                 # 转置',
        'A^*                 # 共轭转置，复数矩阵常用',
        'tr(A)               # trace：主对角线之和',
        'Diag(x)             # 用 x 放到对角线，生成对角矩阵',
        'diag(A)             # 取 A 的主对角线',
        'rank(A)             # 矩阵的秩：有效独立方向数量',
        'A ≽ 0               # A 半正定：v^TAv ≥ 0 对所有 v 成立',
        'A ≻ 0               # A 正定：v^TAv > 0 对所有非零 v 成立',
        'I_d                 # d×d 单位矩阵',
        'P^T P = I           # P 的列向量正交归一',
      ].join('\n'),
      points: [
        'tr(A) 把矩阵压成一个数，很多概率和优化公式用它表达所有对角方向的总量',
        '⊙ 保持逐元素形状，⊗ 会扩张成更大的块矩阵，看到结果形状就能分辨',
        '半正定常用于协方差矩阵、核矩阵和二次型，表达不会产生负方差或负能量的约束',
        'rank 不是元素个数，而是矩阵真正包含多少个独立方向',
      ],
      pitfalls: [
        'A* 在实数矩阵里有时被作者当作最优解，有时是共轭转置，必须看上下文',
        'A≽0 是矩阵之间的偏序关系，不是逐元素比较 A 的每个数是否大于 0',
      ],
    },

    {
      id: 'evaluation-metrics',
      title: '实验指标与统计量',
      lang: 'python',
      tags: ['评估', '常见'],
      why: '实验表格里的缩写也是符号语言。先分清 TP/FP、均值/标准差、置信区间和显著性，才能正确判断一个方法是否真的更好。',
      code: [
        'TP / TN                  # 真正例 / 真负例',
        'FP / FN                  # 假正例 / 假负例',
        'Precision = TP/(TP+FP)  # 预测为正的里面有多少是真的',
        'Recall = TP/(TP+FN)     # 所有真阳性里找到了多少',
        'F1 = 2PR/(P+R)          # Precision 和 Recall 的调和平均',
        'μ ± σ                   # 多次实验的均值 ± 标准差',
        'SE = σ/√n               # 标准误：均值估计的不确定性',
        '95% CI                  # 95% 置信区间',
        'p-value < 0.05          # 在给定假设下观察到结果的概率很小',
        'Δ = score_new-score_base # 相对 baseline 的改变量',
        '↑ / ↓                   # 指标越大越好 / 越小越好，表头必须确认',
      ].join('\n'),
      points: [
        'Precision 想我报出来的有多少靠谱，Recall 想真正的我漏了多少，方向相反',
        'σ 描述实验结果之间的波动，SE 描述均值估计的波动，不能互换',
        'Δ 只说明相对变化，不等于统计显著；还要看区间、重复次数和实验设置',
        '↑/↓ 是表格里最容易忽略的符号：loss 通常 ↓，accuracy 和 F1 通常 ↑，但不能盲猜',
      ],
      pitfalls: [
        'p-value 小不等于效果大，只说明在某个统计假设下结果不太像随机波动',
        '不同论文的测试集、采样方式和 baseline 不同，分数不能脱离实验设置直接横比',
      ],
    },
  ],
};
