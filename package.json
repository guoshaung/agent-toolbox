{
  "name": "agent-toolbox",
  "version": "0.1.0",
  "description": "个人 Agent 工具箱：文档浏览 / 打字纠错 / 专注模式 / DeepSeek 快问",
  "private": true,
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "icon": "electron scripts/build-icon.js",
    "check": "node --check src/main/main.js && node --check src/main/preload.js && node --check src/main/quick-explain.js && node --check src/main/ai-config.js && node --test test/*.test.js",
    "logo": "node scripts/build-logo.js"
  },
  "devDependencies": {
    "electron": "^33.2.0"
  },
  "dependencies": {
    "pdfjs-dist": "^4.10.38"
  }
}
